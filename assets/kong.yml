
_format_version: "1.1"

services:
- name: unknown
  url: https://unknown


  plugins:
  - name: sentry
    config:
      sentry_dsn: https://abcdef0123456789abcdef1234567890@sentry.io/0
      environment_envvar: ENVIRONMENT
      timeout: 10000
      keepalive: 60000
      queue:
        max_retry_time: 30
        max_coalescing_delay: 2
        max_entries: 10000
  routes:
  - name: unknown
    paths:
    - /
